name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Install
        run: yarn
      - name: Percy Test
        uses: percy/exec-action@v0.1.1
        with:
          command: "cypress run"
        env:
          PERCY_TOKEN: aaf4b85a12f08904981a24f851153ad091d1f590abd429c34a9b301682f4d16d
